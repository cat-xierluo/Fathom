# 2026-09-22 近期方向与接手上下文复审

本文件保存固定基线的审查证据；任务状态和当前顺序只在 [TASKS](../TASKS.md) 维护。

## 结论

产品方向总体未跑偏。近期代码主要落实 v0.3.0 所需的后台注册、设置重载、更新入口和原生验证，未见提前引入 M4 智能解释、自动清理或扫描器重写。无需回滚已有成果或重写技术栈。

偏差集中在上下文持续同步和交付结论：一部分入口仍要求重做已合并功能，另一部分又把未接入产品的升级安全流程说成“代码已齐、只剩人工门”。继续照这些入口派发，会同时造成重复工作和必要工作漏项。本次修正事实与接续边界，不调整既定产品或验收目标。

## 范围与方法

- 固定主干 `82b0616c557a42961c07cd9344bd0e64e2a9c6ab`（PR #145），近期范围 `e033ef659bc838dc69ca003f160d58e4569c9c71..82b0616c557a42961c07cd9344bd0e64e2a9c6ab`：28 个提交、42 个文件，新增 8,260 行、删除 207 行。包括上次 ISS-074/075 纠偏及其后接续，不能把所有行数当作产品功能投入。
- 本地 main、origin/main、远端 `ls-remote` 结果一致；`git fetch origin` 首次遇代理 503，重试成功。审查起始远端开放 PR 为空。
- 核对五个既有 worktree：main；旧 `iss-074-review-direction`；在途 `iss-030a-upgrade-fixture` / `review-iss-030a`；旧 `review-iss-016b`。均无未提交改动。030A 两工作区同指 `cde0db778cc4d034ee2b41294208bd85d93eb9f6`；无开放 PR 不等于无在途工作。本次另建 `iss-079-context-convergence`，未修改或清理这些工作区。
- 阅读相关完整父/子任务卡、权威文档、发行准备清单及代码；静态核对 Rust 更新命令、注册桥、设置页与 Python 重载合同。未据作者名判断使用了哪种模型，也未读取其他 PM 私有对话。
- 本次未运行产品测试套件、GUI、打包、实际升级或生产扫描；这些行为均为 `NOT_VERIFIED`。655/105/39/49 等数字是配置或既有任务记录，不是本次实跑结果。

## 审查期间主干前进后的复核

原 PM 在本次修订期间以 #146 → `8f3564d`、#147 → `31396a08ed90a37f787614d6f32024e30836c3aa` 完成 ISS-030A 收口，移除了该实现/review worktree。本分支已基于新主干重放文档修订，保留完整夹具、原验收记录与 TESTING 入口；当前 TASKS 采用 DONE，不再沿起始观察保留 REVIEW_EXTERNAL。以上起始范围统计不包含这两个后来提交。

新主干未修改 `updater_install`，生产接线缺口结论不变；#147 又出现“人工门全集/代码侧仅剩可选小项”的表述，本次同样更正。新增的计数同步缺口：本地 `ci_pytest.sh` 为 671、云端 `ci.yml` 仍 655，已登记 ISS-080。当前 CI 停用不等于两处配置一致。本次没有重跑 030A 或验证其全量通过数。

## 近期变化是否服务目标

| 变化 | 证据 | 判断 |
|---|---|---|
| 后台注册与最小权限 | #131 `2162e4a`、#133 `536e0c4`；`autostart.rs`、`launchd.py` 与设置页 | 属发行主链；原生生命周期与运行根合同仍待收口 |
| 设置持久化后的计划一致性 | #139 `bce84a9`；`service_reload_state` 四态和经确认重装 | 解决真实生效反馈，不应再派“从零实现重载入口” |
| 应用更新入口 | #143 `10bcf72`；updater/process、应用级 ACL、确认流 | 必要代码切片；不等于安全升级流程已完整 |
| Tauri 三尺寸与 Esc | #125 `4181fd2`、#130 `18b5216` | 对应已有原生验收缺口；14 项与 Esc/Tab 实测仍不能补勾 |
| 候选指纹登记 | #140 `87f6ea7`；`release_candidate_record.sh` | 有固定候选价值；工具通过不等于已重建新候选 |
| 升级协议夹具 | #145 仅登记任务；030A 在途分支有测试实现 | 可为协议验证准备证据；不能替代生产更新接线 |

## 可复核的偏差

### P1：将产品实现缺口写成只剩人工门

基线 TASKS 当前摘要称“发行主链代码侧已齐”。但 [updater_install](../../apps/desktop/src-tauri/src/lib.rs) 的调用链是：取得已检查候选 → 发 downloading 状态 → `download_and_install(|_, _| {}, || {})` → 发 installed 状态；前置未调用扫描停写、旧 helper 退出、一致备份或失败恢复。`updater_restart` 只请求重启，既有退出回收不能倒推为安装前协调。下载进度回调为空，确认层的“取消”也不等于安装过程中可以取消。

这不是声称已经发生数据损坏，而是现有源码不足以满足 ISS-040/030/TESTING 已要求的完整路径。030A 范围明确不改产品代码，其 fake app/helper 流程即使全绿，也不会自动改变 `updater_install`。

修正：保留 040B 已验收的插件/ACL/状态切片；生产接线与真实模块集成合同归 ISS-040，恢复与实包验证归 ISS-030。G10 安装前先满足实现前置；检查和离线状态等独立验证可先做。

### P1：权威入口同时包含反方向的过期事实

- DESIGN 首段仍写 ISS-026 待用户确认、027/028 尚未完成；正文又记录原型已合并、R3 已实装。
- TASKS 同时保留“010B/016B/040B 全 DONE”与“下一步实施 010/016，再进入更新”的两条当前顺序；ISS-076 清单 §4 仍把注册/重载/updater 写成未实现，§6 还建议新建已交付的 010B。
- ARCHITECTURE 标题基线仍为 09-19，正文已追加 09-22 行为；TESTING 的当前 pytest 数仍为 639，脚本与 CI 配置均为 655。
- TESTING 的候选命令末尾以 `三反引号紧接“矩阵各行”` 开头，未形成合法结束围栏，后续矩阵归属和证据格式可能被渲染成代码。

修正：当前事实按同一固定基线刷新，旧原型和派发过程明确为历史；当前顺序只留 TASKS，操作清单只保留步骤与承接指针。修复围栏，保留未验证边界；该围栏后亦由 #146 修复，最终保留双方一致结果，不重复算作本 PR 新修复。

### P1：索引与交付状态脱节

- ISS-078 索引 REVIEW，卡头已 DONE 且 #140 已合并（远端再次只读核实 MERGED）：修正为 DONE。
- ISS-030A 索引 READY/“尚未执行”，实际已有实现与 review worktree：起始阶段记录为在途；随后原 PM 已合并，最终文档采用 DONE 并保留其证据，不代替原 owner 做验收。
- ISS-077 的代码 #130 已合并，索引仍 REVIEW，但真实 Esc/Tab 未测；ISS-028 也只有等待前台的残留验证却列 READY。两项记 WAITING，保留所有必要未勾项；恢复条件是允许的前台窗口，不是重新实现。

修正只改变记录与领取判断；不补造 ACCEPT、运行证据或业务完成结论。

### P2：新增能力未写入用户变更记录

`git diff e033ef6..82b0616 -- CHANGELOG.md` 为空，近期注册/重装/更新区、扫描状态指示与 Esc 接线未入 `[Unreleased]`。本次按代码与验收范围补记，并明确生产更新源关闭及剩余验证，未把内部夹具包装为用户功能。

### P2：历史流水再次进入接手入口

扫描 run 7/8 的整段观察被追加到首页；卡中仍有已被后续合并取代的“接手后派 reviewer 审旧 head”指令。容易误把旧时间窗、旧 provider 和旧 PR 当当前任务。部分准备清单还把既有隔离 draft/key 配置授权重复列成一律新授权。

修正：观察归回 ISS-001；028 的旧派发包进明确的历史区；076 卡标注旧证据适用范围；授权表只核对缺失输入和具体环境，不重复索取已给授权。生产注册、公开发布、对外发送仍遵守原边界。

## 文档体检的适用限制

按 doc-curator 的隔离子代理流程运行全量与历史检查。项目无专用配置，临时配置映射现有文档；未改共享 Skill state。

- 历史检查 exit 2：检测到 TASKS/DECISIONS 有更新而 CHANGELOG 未更新，人工复核为真实缺口。
- 全量 exit 1：原始 hard 108 主要来自相对链接按仓根解析、未剥锚点及把代码路径/产物当文件链接；不等于 108 个实际坏链接。行数和“87 活跃任务”是未过滤 DONE 的粗计数，不作为跑偏结论。
- 扫描器另有 `unbound variable`、macOS awk 语法错误；相关归档/active-zone 检查为 `NOT_VERIFIED`，不采信其假绿输出。项目不把任务历史迁入 DECISIONS，也不为满足工具格式虚构决策。
- 原始日志临时位于 `/private/tmp/fathom-doc-curator-hsx0zhmz/`；本文件保留足以独立理解的匿名摘要，不要求后续 Agent 访问该临时目录。最终文档另按仓库相对路径、锚点及任务关系验证。

## 修订与验证边界

本次只修正既有文档和 ISS-079 记录，不改产品行为、不恢复自动化、不代替在途 030A 的独立审查。文档验证结果及本次 PR 状态回写 ISS-079；不会把本文当作第二份任务队列。

最终文档复核：独立 reviewer 对 `7ce3d73795716b5b2811877cc83caf59ec0b3ef6`（base `31396a0`）给出 ACCEPT、0 blocking；10 份 Markdown、84 个相对链接/锚点、89 组任务索引/完整卡、依赖与围栏检查通过，Git 差异无空白错误。其后只补本验收记录与 TASKS 的 REVIEW/PR 指针；完整修订见 [PR #148](https://github.com/cat-xierluo/fathom/pull/148)。业务、实机与发行验证仍为 NOT_VERIFIED。
