# 变更记录

本文件是用户可见变更的权威记录。格式遵循 legal-skills 的 CHANGELOG 规范。
（0.2.0 及以前项目名为 disk-sentinel / 容量哨兵，0.3.0 开发线起更名 Fathom，历史条目保留原名。）


## [Unreleased]

### DMG 安装界面与首次打开放行提示（ISS-009 / DEC-022）

- v0.3.0 不做 Developer ID 签名与公证（用户决策，参照 Folia 分发模式）：DMG 安装窗口改为带安装引导的自定义背景——拖拽 Fathom 到 Applications 的箭头指引，以及「首次打开提示」：说明本应用未经 Apple 签名与公证、首次打开可能被 macOS 阻止，并给出两步放行方法（右键点按 Fathom 选「打开」，或前往 系统设置 → 隐私与安全性 点「仍要打开」）。
- 背景图由 `scripts/build_dmg_background.py` 生成（产物入库，日常构建不依赖 Pillow）；打包校验新增 DMG 段断言（背景图 + app + Applications 链接在位），未签名分发的信任边界与放行步骤同步写入 README。

### 打包态主界面 404 修复与实机 GUI 验证（ISS-009 切片 2）

- 桌面 app 此前打开后主窗口显示 `{"detail":"Not Found"}`：`frontend/` 静态资源从未打进 helper 冻结树，页面挂载被静默跳过（`/health` 等接口正常，切片 1 校验只探 `/health` 故未拦截），打包态用户完全无法使用界面。现在打包脚本把 `frontend/` 一并冻结、构建时断言 `_internal/frontend/index.html` 在位，bundle 校验新增「`/` 返回前端页面」防回归断言。
- 实机验证（隔离运行根，生产数据不受影响）：正常启动后总览/变化/目录详情/大文件/设置页与图表完整渲染；候选端口全部被占时握手页如实显示「握手未完成」、端口占用明细与恢复指引，且不向任何进程发信号；退出（含 AppleScript quit）后内嵌后台服务被回收、端口释放。仍待人工验收：新测试账户从 DMG 安装首启、菜单栏图标手点退出、断网首启。

### 扫描根排除列表（PR #95 / ISS-066）

- 新增按目录/文件名字掩码的扫描排除（`settings.json` 的 `exclude_names`，或环境变量 `FATHOM_EXCLUDE_NAMES` 分号分隔）：已知会让扫描挂起或反复消失的第三方容器目录（如微信、WPS、照片图库云同步缓存）可以配置跳过，`du` 不再遍历它们。默认不配置时行为与历史完全一致。
- 排除集是数据集身份的一部分：修改排除列表后形成新数据集、不与旧数据互比（与「同根同阈值才可比」的既有口径一致）；快照库自动迁移（v5，保留备份），日报注明当前排除掩码。
- `/api/status` 新增返回 `vanished_count` 与生效的排除列表；设置接口支持该字段的校验与保存。

### 设置持久化代码切片（ISS-016A）

- 扫描根、计划时间、入库阈值（KB）与低空间提醒阈值（GB）可在设置页查看并修改，保存到运行根 `settings.json`（原子写，失败保留旧文件）并在当前服务进程立即生效，重启后仍生效；无该文件的旧运行根全部走默认值，不迁移不报错。
- 无效输入（时间非 HH:MM、路径不存在或非目录、阈值非正的有限数值）被 400 拒绝并给出中文原因，当前生效值保持旧值可辨；换根形成新数据集，旧数据不删。
- 新增 `GET/PUT /api/config`（PUT 走与既有写方法相同的 Host/Origin/写令牌守卫）；本切片不注册、不重载任何 launchd 服务，保存计划时间后需重装后台计划（`main.py install`）才按新时间运行，设置页如实标注。
- 环境变量 `FATHOM_SCAN_ROOT` 优先于 `settings.json` 的扫描根（运维/隔离合同入口不被落盘设置反超），被覆盖的字段在设置页如实标注来源。

### 长时扫描的可靠性（PR #85 / ISS-064，PR #90 / ISS-065）

- `du` 安全时限改以墙钟计（并保留单调时钟作第二轨）：此前 macOS 合盖睡眠期间时限不流逝，叠加个别目录（如第三方应用容器内挂起的文件对象）让 `du` 阻塞无输出，一次扫描可无限期挂起并占用扫描锁；现在时限到达即回收 `du`、释放锁、记录「已中断」，报文附上 `du` 当时所在目录的线索便于排查。
- 扫描期间被系统清理的目录（缓存、临时文件等）不再让整次采集作废：此前 8 小时扫描因 30 个「记录时存在、校验时已消失」的目录被整体丢弃；现在这类目录单独计数（`vanished_count`，快照 schema 升至 v4，旧库自动迁移并保留备份），快照照常写入，日报与通知会与权限受限一起如实注明「另有 N 个目录在扫描期间已消失」，不冒充完整覆盖。真正无法解析的记录仍会使采集作废。

### 命令行提示与后台服务只读状态（PR #79 / ISS-062，PR #88 / ISS-063，PR #87 / ISS-010A）

- 扫描开场提示基于上次实测耗时与本次安全时限（含无效值兜底），不再写死"5-15 分钟"。
- 新增只读查询登录项/后台计划状态的桌面命令与 dry-run 预览（不注册、不写任何系统配置；登录项状态暂如实显示"未知"），供后续设置页使用。

### 大目录定时扫描不再在 1 小时被中断（PR #77 / ISS-061）

- 单次 `du` 采集的安全时限从写死的 3600 秒改为可配置的 `FATHOM_DU_TIMEOUT_S`，默认 14400 秒（4 小时）。此前百万级目录的用户主目录连续两天在整 1 小时被中断，当天快照与日报静默缺失；现在同规模扫描有 4 小时预算，超时仍会被明确记为「已中断」并保留上一次有效快照，不会留下半写数据。
- 非法配置（0、负数、非数字、`nan`、`inf`）会在启动时报错，不存在"无限超时"的配置路径；与瞬时错误（EINTR，ISS-047）的处理彼此独立。
- 真实生产规模下 4 小时是否足够，以 2026-09-16 起的定时扫描观察为准。
- 命令行扫描的开场提示（PR #79 / ISS-062）不再写死"5-15 分钟"：有同根历史快照时显示上次实测 `du` 分钟数，否则明确写"首次或无实测记录"，并始终给出本次安全时限与调整方法；提示只读取历史数据，读取失败不影响扫描。

### 桌面壳内嵌 helper 与未签名打包流水线（PR #61 / ISS-009 切片 1，含 ISS-053/054/055/057）

- Tauri 壳在打包态启动时拉起内嵌的 PyInstaller 冻结 helper（`Contents/Resources/helper/`），经 `helper-instance.json` 与 `/health` 身份握手后导航到本地服务；同服务已在运行则复用、不重复拉起；端口被占用时按 ISS-029 语义让位且不向任何外部进程发信号。
- 退出回收覆盖所有路径：tray 菜单退出、Cmd+Q、AppleScript quit、系统注销等都通过同一幂等钩子回收本壳拉起的 helper（SIGTERM，10 秒上限），复用模式不发信号，不再留下被 launchd 收养的孤儿 helper。
- 握手更健壮（PR #71 / ISS-059）：壳读到身份匹配的 `helper-instance.json` 后先探测 `/health`，上次崩溃残留的陈旧文件不再把窗口导航到已死端口，而是重新拉起 helper（判定只读、不向任何进程发信号）；候选端口全部被占时握手页显示端口占用表与恢复提示并可重试，不再显示为笼统错误。
- 新增 `scripts/build_helper.sh` / `build_app.sh` / `verify_app_bundle.sh`：产出未签名 `.app` 与 `.dmg`，校验 bundle 结构、只读布局指纹、启动就绪、端口冲突让位、零击杀、二次启动与退出清理（当前 12 项全部通过）。构建脚本在 tauri build 失败时不再被残留产物掩盖为成功；无 helper 产物时 `cargo check` 不再被资源 glob 阻断。
- 限制：仅 arm64、未签名未公证、图标为占位（正式 Logo 待 ISS-045）、未在无开发环境的新账户实测、含空格/中文路径与 tray 菜单实机退出未验证；云端 CI 因 GitHub Actions 计费被拒记为未运行，合并依据为本地全量验证与独立 review。合并后一项版本一致性测试夹具因 bundle 配置形态变化失败（337/338），已由 PR #68（ISS-058）改为按 JSON 对象注入嵌套版本键修复，全量 338/338。

### 大文件查询（PR #39 / ISS-032）

- 大文件查询改为显式触发的受预算任务：同参数并发只启动一次 `find`，取消或超时后子进程被回收；结果带 TTL 缓存（过期后再次查询会重新扫描），命中上限时明确标注截断；无匹配、无权限、失败、截断、缓存命中/过期分别可辨，`find` 的错误输出与非零退出不再被当作“没有大文件”。
- 查询响应携带范围（根/天数/阈值/上限）与资源统计（墙钟、峰值内存、输出行数）；页面展示进度、范围、失败与截断状态；日志中的路径经脱敏，不记录真实完整路径。
- 命令行与日报的大文件路径对过期缓存不再静默输出旧列表。

### 界面实装（PR #49 / ISS-028）

- 总览、变化、分布、设置四页接入真实扫描事实：总览显示覆盖质量与首次记录/未记录语义；变化页为统一可排序表并提供目录详情侧栏；分布行可聚焦、长路径一键复制；设置页显示运行历史；卷容量走势图提供等价表格；三种窗口尺寸无横向溢出；键盘 Tab 顺序与 Esc 焦点返回可用。
- 变化页“净变化”改为根同口径差分（与“根目录 X → Y”同源），缺基线时显示“无基线”而不再对列表行求和；列表行值不可累加的说明保留。

### 版本与开源准备（PR #57 / ISS-037）

- 版本统一为 0.3.0 并建立单一来源：API 文档、Tauri、Cargo 与 Cargo.lock 本地包行同源，新增一致性校验器在任一漂移时失败；新增依赖来源清单、第三方声明、许可证选项方案、贡献与安全反馈说明草案。
- 项目采用 Apache License 2.0（与 Folia 一致），仓库根新增 LICENSE，Cargo 包声明 license 字段；这不改变仓库当前的私有托管状态。

### 小修与加固（PR #52–#56 / ISS-048~052）

- 命令行 `report` 与 API/日报一致按同数据集前驱生成，可用 `--snapshot-id` 指定，无前驱时明确提示。
- 大文件查询的调试日志不再输出真实完整路径。
- 过期日报与日志按保留天数在扫描收尾清理（文件名日期不可解析的一律保留）。
- 冻结冒烟脚本改为直接记账目标进程，退出码判定不再依赖子 shell 传播。

### 自包含运行时与后台服务（PR #45 / ISS-029 切片 2）

- 冻结 helper 的生产路径打通：对象方式导入 uvicorn 应用后，PyInstaller 产物无需 hidden-import 即可服务；运行目录（data/reports/logs）落在冻结树之外，冻结树内容指纹保持不变。
- 新增 `--version` 与 `/health`（service/protocol_version/pid/port/runtime_mode/status），身份常量单一来源，供同服务探测与排障。
- 端口被占用时按身份让位：同服务实例让位后退出 0；未知进程占用时让位到范围内空闲端口，**全程不向任何外部进程发信号**（零击杀）；端口发现文件 0600 且按身份清理。
- 验证：冻结冒烟 21/21（含让位与零击杀反例）、合同 pytest 19 项、全量 295 通过。

### 采集可靠性（PR #34 / ISS-047）

- du 采集遇到瞬时系统错误（`Interrupted system call`、`Resource temporarily unavailable`）不再使整天快照丢失：瞬时错误单独计数并使采集诚实归为部分覆盖，与真实致命错误并存时仍拒绝写入；修复了 2026-09-13 定时扫描因两行 EINTR 被整体拒绝的生产问题。

### 查询口径（PR #35 / ISS-024）

- 卷趋势与路径趋势改为取最新 N 个点后按时间正序返回，并按数据集（根 + 入库阈值）隔离，不再混入其他根或阈值口径的历史点；缺失点保留空缺不补造。
- 目录树响应携带 `truncated/matched_count/node_count/node_limit`，节点预算在查询层生效，截断时子孙提升为顶层且无重复；指定快照不存在返回 404、空库返回空快照、低于显示阈值返回空结果而非报错；根目录 `/` 不再成为自己的子节点。
- 所有查询参数校验失败统一返回 400 并附中文说明（原 422）。

### 数据口径与差分（PR #31 / ISS-021）

- 差分、日报与 `/api/diff`、`/api/browse` 现在只比较同数据集（同根同入库阈值口径）的有效快照：显式跨根或跨阈值配对返回 400，同数据集前驱不足返回 409；`/api/browse` 无同数据集基线时差值为 null，不再把当前大小冒充增量。
- schema v2→v3：快照持久化入库阈值与采集质量（full/partial），旧库经事务迁移升级且旧行不补造未知元数据，迁移失败自动回滚并保留原库。
- 保留策略与同日替换按数据集分组：两根或双阈值口径的历史互不混用、同周各保留一份；更换阈值后同日共存。
- 日报报头明确对比快照 a/b 编号与记录口径；跌破阈值的目录如实表达为「未记录（不构成删除证明）」，新出现的大目录表达为「首次记录」，不再冒充文件系统事实。

### 界面结构（PR #29 / ISS-027）

- 前端拆分为无构建链原生 ES modules：请求（响应世代号防旧响应覆盖）、格式化、图表（隐藏后重显尺寸正确）、轮询（幂等单实例）、Tauri 桥（浏览器静默降级）与五页生命周期模块，hash 路由提供单一刷新入口；五页原有行为与本地资源加载保持不变。

### 开发与验证（PR #29/#31/#34/#35/#45/#49/#52–#57）

- 精确 pytest 门禁 179→209→260→295→338→285（ISS-024 新增 30 项查询口径测试、ISS-047 新增 21 项瞬时错误分类测试、ISS-032 新增 25 项大文件任务测试）；前端检查 33→38 项；原 179→209（新增 30 项差分语义测试，全部合成隔离数据）；安全浏览器夹具造数改用生产默认阈值口径，与 API 扫描同数据集；前端模块/生命周期检查扩至 33 项真实 Chromium 用例。

### 安全与数据完整性（PR #8/#9/#13/#19/#21/#25）

- 修复差分父子折叠在达到 Top-N 后过早停止、从而遗漏更精确子目录或独立高排名目录的问题；现在会在完整候选集上折叠，再稳定排序并限制结果数量。
- 无效 `du` 采集不再覆盖同日有效快照：致命错误、空输出、缺失根记录、信号退出、负数及混合错误会在写入前失败；仅有明确权限拒绝且根记录有效时可作为部分覆盖，并记录真实扫描耗时。
- 权限错误按 stderr 的实际错误消息段精确分类，目录名中的 `Permission denied` 等文字不能冒充权限证据；写入中断继续由事务回滚保护旧快照。
- 本地 HTTP 服务限制回环 Host 和允许的 Origin，写请求使用进程内随机令牌；Finder 显示会规范化路径并拒绝越界、符号链接逃逸、相似前缀根及无效 JSON。
- 页面输出与报告路径补充转义和精确校验；应用页面使用严格 CSP，API 文档仅在精确文档路径获得所需资源策略。
- BSD `du` 输出改为按原始 bytes 与实际根前缀映射，tab、换行、中文、字面反斜杠和八进制外观路径可准确写入 SQLite；歧义或无法解码的路径会拒绝采集，不再猜测改写文件名。
- 增加单一运行配置与 schema v2：数据、报告、日志、扫描根、资源根和端口可完整隔离；旧 v0/v1 库迁移前生成包含已提交 WAL 的一致 0600 备份，损坏、未来版本或结构不兼容的库会明确拒绝打开。
- API、CLI 与定时任务现在共享同一跨进程扫描生命周期：重复触发会返回忙碌，首扫无对比日报仍成功；报告或通知失败不会抹掉有效快照，取消与退出会回收本次启动的 `du`。

### 界面修复（PR #20）

- 修复增减值符号、错误图标源码泄露和重扫后快照选择过期；总览会正确表达仅首次/不再记录的变化。
- 页面请求加入按视图和导航隔离的响应世代控制，较慢的旧响应不会覆盖较新的错误、选择或页面；补充重扫、404 恢复、首扫/断网/500 与按钮可访问名称。

### 开发与验证（PR #23/#26）

- 将 pytest 精确数量门禁随已交付测试集从 168 项同步为 179 项；默认数量和显式覆盖不一致都会非零退出，避免新增测试后本地与未来 CI 因旧计数持续假红。

### 功能与修复（Wave 1，PR #3/#4/#5）

- 日报写完后尝试发送扫描通知；摘要显示增长及首次记录的大目录、剩余容量，低于 10 GB 时带提示音。修复首次记录大目录被“无增长”掩盖及过长路径摘要问题；命令成功不代表系统实际展示。
- 手动扫描状态与历史持久化到 scan_runs。该阶段修复线程启动失败遗留锁和 failed 状态落库；后续 PR #25 已统一跨进程协调与首扫语义。
- tray 增加明确的远程命令授权、失败去重与恢复提示；统一图标、菜单和状态推送到唯一实例，状态行随推送更新。系统通知与原生菜单的完整真机验收仍待完成。

### 规划与文档
- 建立可复现 macOS CI：原生 Apple Silicon 与 Intel 分别运行 144 项 pytest 和 Rust locked build，Apple Silicon 另跑 39 项 Chromium/API 安全检查；依赖按运行/开发分层并固定已验证闭包。
- 锁定 v0.3 helper 的 PyInstaller onedir 方向，加入可复跑的进程所有权实验和安全缺口清单；该实验尚未接入 `.app`，不代表已有自包含安装包。
- 面向可安装桌面应用重排路线，补齐扫描可信度、UX/UI 原型与实装、运行时分发、升级恢复和开放发布门槛。
- 将 v0.3.0 明确为首个可分发目标，新增应用内更新、双架构 Developer ID 签名、Apple 公证、draft Release 与真实 N→N+1 恢复门禁；当前代码仍是开发线，不把原型或未自包含桌面壳声明为已发布应用。
- 复现并登记扫描失败覆盖、首扫误报、路径解析、差分口径与前端状态问题；保留 ISS-003/007/008 既有分支接续，不把规划写成已修复功能。
- 补充依赖来源/用途识别、可选 Agent 解读及目录标签的远期合同，明确证据、未知、数据发送与执行边界。
- 新模型可按任务依赖、实施范围、反例和验证入口接手；纠正 FATHOM_DB 隔离范围和保留期说明。复审补充 Wave 1 冲突编号的历史映射与夹具实例身份确认，避免领错任务或误测生产服务。本项只变更文档，应用行为未改变。

### 0.3.0 开发检查点（未发布，2026-09-12）

#### 改进
- **更名 Fathom**：延续 Folia/Funes 的 F 系列命名。fathom（英寻）是水深测量单位，动词意为"测深、弄明白"——双关直击产品要回答的问题："I can't fathom where my disk space went"。涉及：项目目录、Python 包、launchd 标签（com.maoscripts.fathom-*，旧任务自动卸载）、Tauri identifier/productName/crate、全部文档；数据库文件平滑迁移（disk.db → fathom.db，保留首扫基线数据）
- **UI 全面去 emoji，对齐 Folia 视觉规范**：导航/面板/按钮/日报全部改用内联 SVG 线条图标（lucide 同款 stroke 风格，currentColor 跟随语义色），Markdown 日报标题同步去 emoji
- **文档层对齐**（同日补）：README/DESIGN 残留装饰 emoji 清理；DESIGN 版本头升至 v0.3；ARCHITECTURE API 清单补齐 v0.2.0 四个端点（browse/reports/reports/{date}/reveal）并在架构图标注桌面壳入口；ROADMAP 与 0.3 开发里程碑对齐；AGENTS 模块速查补 icons.js；清理空的 docs/ux/ 遗留目录
- **任务治理升级（TASKS.md）**：全部任务改为固定字段合同（目标/前置/范围/要点/验证/验收框），新增依赖与并行轨道图（观察/壳/后端/功能四轨）、`iss-NNN-slug` 分支规范与自动认领规则；ROADMAP 条目全部立项（新增 ISS-015 周报月报、ISS-016 设置可写、ISS-014 窄屏适配）

## [0.2.0] - 2026-09-12

### 新增
- **Tauri 2 桌面壳**（apps/desktop/）：macOS 菜单栏常驻图标（圆环 template 图标 + 剩余 GB 标题，由前端心跳推送）+ 原生主窗口（1220×820，居中）；左键点 tray 图标打开窗口；tray 菜单：状态行 / 打开主界面 / 立即扫描（触发前端扫描）/ 退出；关闭窗口=隐藏（菜单栏应用惯例）
- **目录浏览器**（分布页）：面包屑下钻、子目录表（大小/较上快照变化/占比）、行内 🔍 在 Finder 中显示、侧栏目录趋势小图；旭日图点击与浏览器联动定位（QDirStat 双栏联动模式）
- **历史日报档案**（变化页）：reports/*.md 列表化回看
- **群晖式信息架构重构**：单页纵向堆叠 → 左侧导航五页（总览/变化/分布/大文件/设置），hash 路由；总览页新增"今日摘要"（结论先行）
- 后端新 API：`/api/browse`（目录浏览器数据）、`/api/reports`（日报列表/内容）、`/api/reveal`（Finder 显示，越界拒绝）；CORS 允许 Tauri loader 页探测
- **内部研究文档体系**（docs/research/，已 gitignore 不入库）：群晖存储空间分析器 UX 与功能映射、五个开源项目（duc/disktracker/QDirStat/WinDirStat/DaisyDisk）功能抽取与编排分析
- **DESIGN.md 升级为 UX 合同**（参照 badminton-lab 方法论）：UX 总纲（按任务组织/结论先行/单一真值/状态全覆盖）+ 信息架构合同 + 五页职责合同 + 状态约定
- tray 图标生成脚本（scripts/make_tray_icon.py，纯 stdlib PNG 编码）

### 改进
- 快照对选择在页面切换后保留用户选择
- 大文件表上限提到 200 行（对齐群晖"最多 200 个"）

### 待办事项
- tray 标题（剩余 GB）与 tray 菜单实际显示效果待用户菜单栏实测（ISS-008）
- .app 打包（tauri bundle）与 Dock/启动台图标待做（ISS-009）
- 文件类型分布、重复文件检测列入路线图（v0.3）

## [0.1.0] - 2026-09-12

### 新增
- 每日快照内核：调用系统 `du -xk` 扫描 `$HOME`，目录级大小（≥10MB）存入 SQLite；同日重复扫描自动覆盖，保留策略为近 35 天每日 + 更早每周一份（最多 12 周）
- 差分引擎：Top 增长/缩减（父子折叠）、新增目录（≥100MB）、消失目录四类视图
- Markdown 日报：每次扫描后自动生成到 `reports/YYYY-MM-DD.md`，含卷剩余变化与无权限目录提示
- 近期大文件查询：`find` 近 N 天修改的 ≥100MB 文件
- Web 仪表盘（http://127.0.0.1:7952）：状态卡（剩余空间低量红色告警）、卷容量趋势、快照对比条形图、当前占用旭日图（点击下钻 + 目录历史趋势联动）、大文件表、手动扫描按钮
- FastAPI 服务：9 个 API 端点，自动文档在 `/docs`
- launchd 部署：每日 12:00 自动扫描 + 常驻 Web 服务（KeepAlive 自动拉起），`install`/`uninstall` 一键管理
- 9 个单元测试覆盖扫描、差分折叠、保留策略、路径转义还原

### 已知限制
- 扫描 `~/Library` 受 TCC 保护目录需授予完全磁盘访问权限，未授权时如实标注无权限数（实测仅 6 个目录受限，影响很小）
- APFS clone 文件的共享块会被 du 重复计量（与群晖行为一致，属已知偏差）
- venv 绑定 Homebrew Python 3.14，brew 大版本升级后需重建（见 README 故障排查）
