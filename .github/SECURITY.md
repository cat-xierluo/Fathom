# 安全说明（草案）

> 本文件是 ISS-037 交付的安全政策草案；漏洞反馈渠道当前为占位，
> 项目公开前不会填入真实联系方式。

## 支持范围

当前安全修复针对 v0.3 开发线的最新 main 与官方 Release 产物；
更早的开发版不承诺回补。

## 架构安全边界（当前事实）

- **全部本地，无网络服务暴露**：内嵌 Python helper 只绑定回环地址
  （127.0.0.1），Host 校验拒绝非本机别名请求；CORS 仅放行 Tauri 壳
  自身来源。无账号体系、无遥测、无崩溃上报，数据不出本机。
- **写入面最小**：快照与查询结果写入运行数据根下的 SQLite，报告/
  日志有保留策略；不主动删除用户文件。
- **凭据**：应用与更新链路的签名/公证密钥只存在于维护者的受控环境，
  不进入仓库与聊天渠道（发行方案约定）。仓库中不应出现任何密钥或
  Token。

## 漏洞反馈

（占位：项目对外公开后在此填写安全联系邮箱与 PGP 公钥指纹；当前阶段
请通过私有仓库的私密渠道联系维护者。）

请不要在公开 issue 中提交未披露漏洞细节、密钥或可被利用的路径样本。

## 匿名诊断与日志（如实说明，不夸大）

- **没有远程诊断上传功能**：不存在"一键上传日志/数据库"的通路，
  整库上传自设计起就被禁止。
- **本地日志脱敏现状**（ISS-032 落地）：大文件查询日志使用
  `sha8+basename` 脱敏（哈希前缀＋文件名），不记完整路径；日志与
  报告各有保留天数策略。
- **已知限制（已登记，勿误读为已解决）**：DEBUG 级别存在含扫描根
  路径的 `key=` 字段（扫描根本身经本地 API 公开，文件级路径未泄漏）；
  诊断导出功能尚未单独实装。提交 issue 附日志时请自行抹去剩余路径
  与机器名。

## 报告里请包含

- Fathom 版本（单一版本源，见"关于"或 `/api/status` 的 version 字段）
  与 macOS 版本/架构；
- 复现步骤与预期/实际行为；
- 涉及崩溃时附（脱敏后的）相关日志片段。
